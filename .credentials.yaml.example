# ============================================================
#  Credentials template - copy to .dsh/.credentials.yaml and
#  fill in your API keys.
#
#  IMPORTANT: .dsh/.credentials.yaml is git-ignored. Never
#  commit real keys. This template file is safe to commit.
# ============================================================

# Provider: opencode-go (agent-default-model in .dsh/settings.yaml)
OPENCODE_GO_API_KEY: ""

# DeepSeek API key (used by the deepseek provider)
DEEPSEEK_API_KEY: ""
